Backend: Node.js + Express scaffold (to be initialized).

Next steps:
- `npm init -y` in `backend/`
- install `express`, `dotenv`, `bcrypt`, `jsonwebtoken`, `prisma`, `@prisma/client`
- create `prisma/schema.prisma` and run `prisma migrate`

Environment: use `.env` (add to .gitignore). See `.env.example`.
